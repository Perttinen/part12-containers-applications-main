version: '3.8'         

services:
  app:                   
    image: express-server 
    build:
      context: .
      dockerfile: dev.Dockerfile 
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules           
    ports:            
      - 3001:3001