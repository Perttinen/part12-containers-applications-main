version: '3.8'

services:
  server:
    image: express-server
    build:
      context: .
      dockerfile: dev.Dockerfile
    volumes:
      - .:/usr/src/app
    ports:
      - 3001:3001
    container_name: back
